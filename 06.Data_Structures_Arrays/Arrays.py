strings = ['a', 'b', 'c', 'd']
numbers = [1, 2, 3, 4, 5]

# push
strings.append('e')

# pop
strings.pop()
strings.pop()

# unshift
strings.insert(0, 'x')

# splice
strings.insert(2, 'alien')

print(strings)
